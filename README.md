# PFAS Analysis (Streamlit)

## Run locally

```bash
uv run streamlit run streamlit_app.py
```

By default the app loads `files/clean_format.xlsx`. Use the sidebar to upload a different Excel file or tweak filters.
